spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: user
      password: 1234
      database: todo-db
      uri: mongodb://user:1234@localhost:27017/todo-db

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG